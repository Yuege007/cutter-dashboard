{
  "buildCommand": "cd vue-dashboard && npm run build",
  "outputDirectory": "vue-dashboard/dist",
  "installCommand": "cd vue-dashboard && npm install --include=optional",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
